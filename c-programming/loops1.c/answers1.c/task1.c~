#include <stdio.h>

/**
 * Print out all even numbers within a given range without using if statement.
 * main - entry point
 *Return: Always 0
 */
int main(void)
{
	int i, given_num;

	for (i % 2 = 0; i <= given_num; i++)
		printf("%d, ", i);
	return (0);
}
